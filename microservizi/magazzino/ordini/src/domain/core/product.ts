export class Product {
    constructor(
        id: number,
        name: string,
        unitPrice: number,
        quantity: number,
        minQuantity: number,
        maxQuantity: number
    ) { }
}